import { JoinArrayKeyPipe } from './join-array-key.pipe';

describe('JoinArrayKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinArrayKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
