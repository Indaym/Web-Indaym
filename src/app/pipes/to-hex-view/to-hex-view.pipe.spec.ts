import { ToHexViewPipe } from './to-hex-view.pipe';

describe('ToHexViewPipe', () => {
  it('create an instance', () => {
    const pipe = new ToHexViewPipe();
    expect(pipe).toBeTruthy();
  });
});
